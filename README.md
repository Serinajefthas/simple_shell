Simple Shell Project in C which performs actions from user input commands similar to that of the shell environmnet in Linux. Such as ls, cd to more complex commands such as manipulating environments of the current child and parent processes
